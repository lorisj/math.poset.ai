type state = {
    openDelimiters: string[];
}



 

const TokenizeMath = (input: string) => {
    // This will take in the entire text to tokenize. Assume that the start/end is already taken care of.
    // So for now assume start/end are already taken care of.
    // This should process stuff inside $here$.
    
    


};



export default TokenizeMath;
